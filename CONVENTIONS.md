# 计算汇总站点约定

后续新增计算工具统一按此逻辑：

1. **首页** `index.html`：计算汇总，用卡片入口选择工具
2. **每个工具**独立子目录，例如 `condensation/`、`screw/`、`新工具英文短名/`
3. 子目录至少包含：`index.html` + 自有样式/脚本（相对路径）
4. 子页顶部提供「← 返回计算汇总」链回首页
5. 纯静态，可 GitHub Pages 公开；不依赖后端登录

新增步骤：在子目录实现工具 → 首页 `cards` 增加一张入口卡片 → 提交推送。
